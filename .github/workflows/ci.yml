name: Node.js CI

on:
  push:
    branches: [ main, master ]
  pull_request:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install and run tests (Node / fallback)
        run: |
          echo "Running repository tests"
          if [ -f package.json ]; then
            echo "Detected package.json — running npm ci && npm test"
            npm ci || npm install
            npm run build --if-present
            npm test
          elif [ -x ./ci/test.sh ]; then
            ./ci/test.sh
          elif [ -f ./ci/test.sh ]; then
            bash ./ci/test.sh
          else
            echo "No test script found — nothing to run"
          fi
